---
description: Инициализировать структуру Memory Bank для проекта. Создаёт файлы всех уровней памяти.
allowed-tools:
  - Bash
  - Write
  - Read
---

# Инициализация Memory Bank

Инициализируй систему Memory Bank для этого проекта.

## Автоматическая инициализация
!`python3 "$CLAUDE_PROJECT_DIR/.claude/hooks/memory-sync.py" --init 2>/dev/null || echo '{"error": "Скрипт не найден"}'`

## После инициализации

1. **Заполни projectbrief.md** — спроси пользователя:
   - Как называется проект?
   - Какую проблему решает?
   - Кто целевые пользователи?

2. **Заполни techContext.md** — определи автоматически:
   - Проверь `package.json`, `requirements.txt`, `go.mod`, `Cargo.toml`
   - Определи основной язык и фреймворки
   - Запиши команды сборки/запуска

3. **Покажи сводку** — что создано, что нужно заполнить вручную

## Важно
- Не перезаписывай существующие файлы
- Держи начальный контент минимальным
- Предложи пользователю заполнить критичные поля